#pragma strict

var times : Array;
var CountReachedDestination : boolean;
var CountFlow : boolean;
var flowZone : FlowZone;//oj behövs inte!!

private var carArray; //hur hittar man den nu igen? :(

function Start () 
{
	if(CountReachedDestination)
	{
		times = new Array();
	}
	if(CountFlow)
	{
	
	}
}

function Update () 
{
	if(CountReachedDestination)
	{
//		for(c in carArray)
//		{
//			var car = c as AICar_Script;
//			if(car.getReachedDestion()!=0)
//			{
//				times.push(car.getReachedDestion());
//				car.setReachedDestion(0);
//			}
//		}
	}
	if(CountFlow)
	{
	
	}

}